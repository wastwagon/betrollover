/** Absolute floor/ceiling for Acca Generator leg settings (admin + runtime). */
export const ACCA_GENERATOR_LEGS_MIN = 1;
/** Heavy match-day analysis ceiling; admin can set a lower live max. */
export const ACCA_GENERATOR_LEGS_MAX = 200;
