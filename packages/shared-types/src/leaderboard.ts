/**
 * All-time public leaderboard + homepage "Leading ROI" use the same floor:
 * only tipsters with at least this many settled picks (wins + losses) compete
 * for top positions; others rank below by ROI among themselves.
 */
export const LEADERBOARD_MIN_SETTLED_FOR_PRIMARY_RANKING = 10;
