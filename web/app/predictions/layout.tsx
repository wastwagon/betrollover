import type { Metadata } from 'next';
import { SITE_URL, getAfricaAlternates } from '@/lib/site-config';

export const metadata: Metadata = {
  title: 'Smart Coupons | AI Predictions',
  description:
    'High-confidence Double Chance football predictions generated by our advanced AI engine. Daily smart coupons with 70%+ win probability.',
  alternates: {
    canonical: `${SITE_URL}/predictions`,
    languages: getAfricaAlternates('/predictions'),
  },
  openGraph: {
    url: `${SITE_URL}/predictions`,
    title: 'Smart Coupons | AI Predictions | BetRollover',
    description: 'High-confidence Double Chance predictions generated by AI. Track our daily smart coupons and win rates.',
  },
};

export default function PredictionsLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
