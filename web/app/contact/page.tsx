import { ContentPage } from '@/components/ContentPage';

export const metadata = {
  title: 'Contact | BetRollover',
  description: 'Contact BetRollover support.',
};

export default function ContactPage() {
  return <ContentPage slug="contact" fallbackTitle="Contact Us" />;
}
