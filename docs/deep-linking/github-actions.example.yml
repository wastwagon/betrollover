# Example only — copy into .github/workflows/ and adjust.
# Injects mobile verification files before Next.js build (see CI_DEPLOY.md).
#
# Required repository secrets:
#   ASSETLINKS_JSON
#   AASA_JSON
#
# name: Web — build and deploy
# on:
#   push:
#     branches: [main]
# jobs:
#   build:
#     runs-on: ubuntu-latest
#     steps:
#       - uses: actions/checkout@v4
#       - uses: actions/setup-node@v4
#         with:
#           node-version: '20'
#           cache: npm
#           cache-dependency-path: web/package-lock.json
#       - name: Write deep-link verification files
#         env:
#           ASSETLINKS_JSON: ${{ secrets.ASSETLINKS_JSON }}
#           AASA_JSON: ${{ secrets.AASA_JSON }}
#         run: |
#           mkdir -p web/public/.well-known
#           printf '%s' "$ASSETLINKS_JSON" > web/public/.well-known/assetlinks.json
#           printf '%s' "$AASA_JSON" > web/public/.well-known/apple-app-site-association
#       - name: Install & build web
#         working-directory: web
#         run: |
#           npm ci
#           npm run build
#       # … add docker push / Coolify webhook / etc.
